#class Quiz:
    #def __init__(self, names, total_score, used_countries, rounds_attempts):
        #self.names = names
        #self.total_score = total_score
        #self.used_countries = used_countries
        #self.rounds_attempts = rounds_attempts